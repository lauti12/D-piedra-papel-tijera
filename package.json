{
  "name": "desafio-ppt",
  "version": "1.0.0",
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /D-piedra-papel-tijera",
    "deploy": "gh-pages -d build"
  },
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.15.4"
  },
  "dependencies": {
    "gh-pages": "^6.3.0"
  }
}
